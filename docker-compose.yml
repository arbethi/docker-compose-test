version: "3"

services:
  next-app:
    build:
      context: ./next-app
      dockerfile: prod.Dockerfile
      args:
        ENV_VARIABLE: ${ENV_VARIABLE}
        NEXT_PUBLIC_ENV_VARIABLE: ${NEXT_PUBLIC_ENV_VARIABLE}
    restart: always
    ports:
      - 3000
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.preprod.rule=Host(`preprod.deepshader.com`)"
      - "traefik.http.routers.preprod.entrypoints=websecure"
      - "traefik.http.routers.preprod.tls.certResolver=letsencrypt"
      - "traefik.http.services.preprod.loadbalancer.server.port=3000"
networks:
  dokploy-network:
    external: true
